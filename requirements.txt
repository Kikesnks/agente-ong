# Dependencias de EJECUCIÓN (runtime) — lo que la app necesita para funcionar.
# Stack definido en .claude/steering/tech.md.
#
# Nota: los adaptadores Claude/OpenAI (langchain-anthropic/langchain-openai) se añaden en
# T4 de SPEC 2; aquí ya está lo que usan el módulo investigador
# (src/agente_ong/research/), la interfaz (src/agente_ong/ui/) y el adaptador Ollama de
# la capa LLM (src/agente_ong/llm/).

# --- Orquestación multi-agente ---
langgraph>=0.2
langchain>=0.3

# --- Adaptadores LLM (src/agente_ong/llm/adapters/) ---
langchain-ollama>=0.2

# --- Cliente HTTP para fuentes oficiales (BDNS, TED), con reintentos ---
httpx>=0.27

# --- Fuentes de búsqueda ---
tavily-python>=0.5
firecrawl-py>=1.0

# --- Lectura profunda propia (R23): extracción del texto principal sin créditos ---
trafilatura>=2.0

# --- Vocabulario ODS (R24): carga de configuración YAML con fallback ---
PyYAML>=6.0

# --- Carga de claves desde .env (ResearchConfig.from_env()) ---
# Declarada aquí como directa: antes solo llegaba como transitiva de firecrawl-py, lo que
# no está garantizado (bug de carga de claves, diagnosticado 09-07-2026).
python-dotenv>=1.0

# --- Interfaz (src/agente_ong/ui/) ---
streamlit>=1.37
streamlit-autorefresh>=1.0  # sondeo del estado de los jobs mientras hay investigaciones activas

# --- El propio paquete (agente_ong, vía pyproject.toml) ---
# Necesario para que Streamlit Community Cloud resuelva los imports (equivale al
# `pip install -e .` de la instalación local).
-e .
